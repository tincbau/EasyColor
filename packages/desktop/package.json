{
  "name": "@easycolor/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "EasyColor for Windows — native camera decoding and H.265 mastering.",
  "main": "dist/main/index.js",
  "scripts": {
    "build:renderer": "cross-env EASYCOLOR_BASE=./ npm run build -w @easycolor/web",
    "build:main": "tsc -p tsconfig.json",
    "build": "npm run build:main && npm run build:renderer",
    "dev": "npm run build:main && electron .",
    "dist": "npm run build && electron-builder --win --publish never",
    "dist:portable": "npm run build && electron-builder --win portable --publish never"
  },
  "dependencies": {
    "@easycolor/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "cross-env": "^7.0.3",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  }
}
